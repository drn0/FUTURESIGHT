import { MessageEmbed } from "discord.js"
export let modEmbed = {
        "title": "modify fields",
        "description": "the emojis go top to bottom, starting at user id. if this report pertains to more than one uid, use fs!rmpush <report> <uids ----> instead.",
        "color": 14861178,
    }